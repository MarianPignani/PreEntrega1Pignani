const ItemListContainer = ({texto}) => {
    return (
        <h2  style={{display: "flex", justifyContent: "center", marginTop: "30px"}}>
            {texto}</h2>
    )
}

export default ItemListContainer;